# Issue Tracker

Issues for this project are tracked using GitHub Issues.

## Workflow
- Use the `gh` CLI to interact with issues.
- The `to-issues` skill creates issues using `gh issue create`.
- The `triage` skill manages issue states by applying labels.
